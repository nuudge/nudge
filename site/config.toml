# Zola config for the nudge build log.
#
# base_url must match where the site is actually served. For GitHub Pages *project*
# pages that's https://<user>.github.io/<repo> (note the /<repo> path prefix). Once you
# point a custom domain at it, change this to the domain root (e.g. https://nudge.dev)
# so image and link paths resolve without the subpath.
base_url = "https://nuudge.github.io/nudge"
title = "nudge build log"
description = "Notes from building a coding agent from scratch in Rust — phone control, multi-client co-op, and an agent that's just another client."
default_language = "en"
compile_sass = false
build_search_index = false

# Zola 0.22 groups code-highlighting under a [markdown.highlighting] sub-table
# (the old top-level highlight_code / highlight_theme keys were removed).
[markdown.highlighting]
enabled = true
theme = "dracula"
